info:
  name: 300-Post-Ticket
  type: http
  seq: 1

http:
  method: POST
  url: http://deep-blue.localhost:{{TenantSwitch}}/api/ticket
  body:
    type: json
    data: |-
      {
        "code": "00001",
        "description": "description",
        "userId": "00000000-0000-0000-0000-000000000001"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
